import DocumentEvents from "./DocumentEvents.js";

const docEvents = new DocumentEvents();

// Export default (singleton instance)
export default docEvents;

// Optionally also export the class itself
// export { DocumentEvents };
